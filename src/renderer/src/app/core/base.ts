import {
  baseIdSchema,
  type BaseId,
  baseEntitySchema,
  type BaseEntity,
  generateId,
  generateTimestamp
} from '@shared/app/lib/base'

export {
  baseIdSchema,
  type BaseId,
  baseEntitySchema,
  type BaseEntity,
  generateId,
  generateTimestamp
}

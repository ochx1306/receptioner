import {
  baseIdSchema,
  type BaseId,
  baseEntitySchema,
  type BaseEntity,
  generateId,
  generateTimestamp
} from '@shared/app/core/base'

export {
  baseIdSchema,
  type BaseId,
  baseEntitySchema,
  type BaseEntity,
  generateId,
  generateTimestamp
}
